# opencode-kit

Monorepo for reusable OpenCode extensions.

## Packages

- [`opencode-peek`](./packages/opencode-peek) — session transcript, token report, and theme-ready HTML rendering.

## Workspace commands

```bash
npm install
npm test
npm run build
npm run pack:peek -- --dry-run
```

Published package documentation lives with each package under `packages/`.
